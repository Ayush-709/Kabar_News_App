<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kabar" parent="Theme.Design.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowLightStatusBar">false</item>


        <item name="android:colorPrimary" tools:targetApi="n_mr1">@color/darkModeInput</item>
        <item name="android:colorSecondary" tools:targetApi="n_mr1">@color/darkModeInput</item>
        <item name="android:textColorPrimary">@color/darkModeBody</item>

        <item name="textAllCaps">false</item>

        <item name="android:textColor">?android:textColorPrimaryNoDisable</item>
    </style>

    <style name="BottomMenuStyle" parent="Theme.AppCompat.DayNight.DarkActionBar"/>

</resources>